# Configs

- `config.json` (root): client configuration; auto-created by scripts/run-client.sh if missing.
- `config/config_client*.json`: sample client configs.
- `config/test_config.json`: test-focused config.
- Environment overrides: coordinator reads PORT/DB_PATH/Google OAuth vars from `.env` or environment.
